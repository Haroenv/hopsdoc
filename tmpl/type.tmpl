<%
    var data = obj;
    var self = this;
    data.forEach(function(name, i) { %>
<span class="__tag param-type param-type-<%= name.toLowerCase() %>"><%= self.linkto(name, self.htmlsafe(name)) %></span>
<% if (i < data.length-1) { %>|<% } %>
<% }); %>
